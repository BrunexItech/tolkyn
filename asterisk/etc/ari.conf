; ARI (REST + WebSocket call control) — off for the POC. The production phase
; enables this so the Tolkyn backend can originate / monitor / transfer calls
; and receive CDR + channel events.
[general]
enabled = no
