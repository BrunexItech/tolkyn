[general]
rtpstart = 10000
rtpend = 20000
; WebRTC (browser) legs use ICE; give Asterisk a STUN server so it can learn
; its own public candidate. The trunk leg (plain SIP/UDP) uses the
; external_media_address set on transport-udp in pjsip.conf.
icesupport = yes
stunaddr = stun.l.google.com:19302
